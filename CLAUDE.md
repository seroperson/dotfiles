# CLAUDE.md

Guidance for working in this dotfiles repo.

## Comments

Keep comments short and only when necessary. Explain *why* something
non-obvious is done (a workaround, a platform quirk, a deviation from the
default) — never restate what the code already says. Prefer a brief trailing
comment over a multi-line block; drop the comment entirely if the code is
self-explanatory.
